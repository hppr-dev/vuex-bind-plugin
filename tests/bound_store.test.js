import BoundStore from '../src/bound_store.js'

test('should return a namespaced vuex store config', () => {
});

test('should return a root vuex store config when no namespace is given', () => {
});

test('should get plugin config from root data store when no namespace is given', () => {
});

test('should generate start_bind action in root namespace', () => {
});

test('should generate state, mutations and actions for each binding', () => {
});

test('should generate actions that dispatch bind/bind', () => {
});

test('should generate start_bind action in current namespace that dispatches all watch and change actions', () => {
});

test('should generate start_bind action in current namespace that watches all necessary endpoint params', () => {
});
