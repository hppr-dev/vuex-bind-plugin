import { RestDataSource, _MockRestDataSource } from '../src/data_sources.js'
import axios from 'axios'

test("RestDataSource constructor should return RestDataSource", () => {
});

test("RestDataSource constructor should have defaults", () => {
});

test("RestDataSource should use axios module", () => {
});

test("RestDataSource args should return arguments for axios", () => {
});

test("RestDataSource assign should extract data from response", () => {
});

test("RestDataSource state should include url and headers", () => {
});

test("RestDataSource constructor should set url and headers", () => {
});

test("RestDataSource mutations should have url and headers updates", () => {
});

test("MockRestDatasource constructor should return MockRestDatasource", () => {
});

test("MockRestDatasource constructor should have defaults", () => {
});

test("MockRestDatasource module should extract mock_data by default", () => {
});

test("MockRestDatasource args should have endpoint and input_params in argument", () => {
});

test("MockRestDatasource assign shouldn't do anything", () => {
});

test("MockRestDatasource state should include url and headers", () => {
});

test("MockRestDatasource constructor should set url, headers and module(transform)", () => {
});

test("MockRestDatasource mutations should have url and headers updates", () => {
});
